// Mentalita — Central de Faturamento
(function(){
'use strict';
const S=()=>window.PLURI_SUPABASE||null,C=()=>window.PLURI_CLINIC?.id||null,U=()=>window.PLURI_AUTH_SESSION?.user||null;
let activeTab='visao',busy=false;
const money=v=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(v||0));
const date=v=>v?new Date(String(v).slice(0,10)+'T00:00:00').toLocaleDateString('pt-BR'):'—';
const esc=v=>String(v??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]));
const labels={draft:'Rascunho',ready:'Pronta para faturamento',batched:'Em lote',sent:'Enviado',processed:'Processado',paid:'Recebido',open:'Em aberto',resolved:'Resolvida'};
const status=s=>labels[String(s||'').toLowerCase()]||String(s||'—');
const CSS=`<style>
#billingWorkspace{width:100%}.billing-shell{display:flex;flex-direction:column;gap:16px}.billing-tabs{display:flex;gap:8px;flex-wrap:wrap}.billing-tab{border:1px solid var(--border)!important;background:var(--surface,#fff)!important;color:var(--text,#182235)!important}.billing-tab.active,.billing-primary{background:#063a59!important;color:#fff!important;border-color:#063a59!important}.billing-kpis{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}.billing-kpi,.billing-card{background:var(--surface,#fff);border:1px solid var(--border);border-radius:14px}.billing-kpi{padding:16px}.billing-kpi small,.billing-stage small,.billing-summary small,.billing-detail small{display:block;color:var(--text-secondary);font-size:10px;text-transform:uppercase;letter-spacing:.06em;font-weight:800}.billing-kpi strong{display:block;font-size:21px;margin-top:6px}.billing-kpi span,.billing-note{display:block;color:var(--text-secondary);font-size:12px;margin-top:4px}.billing-card{padding:18px}.billing-head{display:flex;justify-content:space-between;align-items:flex-start;gap:12px;margin-bottom:14px}.billing-head h3{margin:0;font-size:16px}.billing-head p{margin:4px 0 0;color:var(--text-secondary);font-size:12px}.billing-list{border-top:1px solid var(--border)}.billing-row{display:flex;justify-content:space-between;align-items:center;gap:16px;padding:13px 0;border-bottom:1px solid var(--border)}.billing-main{display:flex;align-items:flex-start;gap:10px;min-width:0}.billing-actions{display:flex;align-items:center;justify-content:flex-end;gap:7px;flex-wrap:wrap}.billing-status{display:inline-flex;padding:5px 9px;border-radius:999px;background:#eef2f6;color:#526579;font-size:11px;font-weight:700}.billing-empty{padding:24px 4px;color:var(--text-secondary);font-size:13px;text-align:center}.billing-check{width:17px;height:17px;margin-top:2px}.billing-summary,.billing-flow{display:grid;gap:10px}.billing-summary{grid-template-columns:repeat(3,1fr)}.billing-flow{grid-template-columns:repeat(5,1fr)}.billing-summary>div,.billing-stage,.billing-detail{padding:13px;border:1px solid var(--border);border-radius:12px}.billing-summary strong,.billing-stage strong,.billing-detail strong{display:block;font-size:18px;margin-top:4px}.billing-stage span{display:block;color:var(--text-secondary);font-size:11px;margin-top:3px}.billing-modal{position:fixed;inset:0;background:rgba(15,23,42,.45);display:flex;align-items:center;justify-content:center;padding:18px;z-index:10000}.billing-dialog{width:min(620px,100%);max-height:90vh;overflow:auto;background:var(--surface,#fff);border-radius:16px;padding:20px;box-shadow:0 20px 60px rgba(0,0,0,.2)}.billing-dialog-head{display:flex;justify-content:space-between;align-items:center;margin-bottom:16px}.billing-dialog-head h3{margin:0}.billing-form{display:flex;flex-direction:column;gap:12px}.billing-form label{font-size:12px;font-weight:700}.billing-form input,.billing-form select,.billing-form textarea{width:100%;box-sizing:border-box;margin-top:5px;padding:10px 11px;border:1px solid var(--border);border-radius:9px;background:var(--surface,#fff);color:inherit}.billing-form-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.billing-dialog-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:18px}@media(max-width:850px){.billing-kpis{grid-template-columns:1fr 1fr}.billing-flow{grid-template-columns:1fr 1fr}}@media(max-width:600px){.billing-kpis,.billing-summary,.billing-form-grid{grid-template-columns:1fr}.billing-row{align-items:flex-start;flex-direction:column}.billing-actions{width:100%;justify-content:flex-start}.billing-flow{grid-template-columns:1fr}.billing-dialog{padding:16px}}
</style>`;
async function rows(table,order='created_at'){const c=S();if(!c||!C())return[];const r=await c.from(table).select('*').eq('clinic_id',C()).order(order,{ascending:false});if(r.error){console.error('[Mentalita Faturamento]',table,r.error);return[]}return r.data||[]}
const guides=()=>rows('mentalita_patient_tiss_guides');
const lots=()=>rows('mentalita_billing_lots');
const receipts=()=>rows('mentalita_billing_receipts','received_at');
const denials=()=>rows('mentalita_billing_denials');
async function lotGuideIds(id){const c=S();if(!c||!C())return[];const r=await c.from('mentalita_billing_lot_guides').select('guide_id').eq('clinic_id',C()).eq('lot_id',id);return r.error?[]:(r.data||[]).map(x=>x.guide_id)}
async function names(ids){const u=[...new Set((ids||[]).filter(Boolean))];if(!u.length)return{};const r=await S().from('mentalita_patients').select('id,name').in('id',u);return Object.fromEntries((r.data||[]).map(x=>[x.id,x.name]))}
async function audit(action,guideId=null,patientId=null,metadata={}){const c=S();if(!c||!C())return;const r=await c.from('mentalita_patient_tiss_audit').insert({clinic_id:C(),patient_id:patientId,guide_id:guideId,actor_user_id:U()?.id||null,action,occurred_at:new Date().toISOString(),metadata});if(r.error)console.warn('[Mentalita Faturamento] auditoria',r.error)}
function modal(html){document.getElementById('billingModal')?.remove();document.body.insertAdjacentHTML('beforeend',`<div class="billing-modal" id="billingModal"><div class="billing-dialog">${html}</div></div>`)}
function closeModal(){document.getElementById('billingModal')?.remove()}
function tabs(){return `<div class="billing-tabs"><button class="btn billing-tab ${activeTab==='visao'?'active':''}" data-a="tab" data-t="visao">Visão geral</button><button class="btn billing-tab ${activeTab==='guias'?'active':''}" data-a="tab" data-t="guias">Guias prontas</button><button class="btn billing-tab ${activeTab==='lotes'?'active':''}" data-a="tab" data-t="lotes">Lotes</button><button class="btn billing-tab ${activeTab==='glosas'?'active':''}" data-a="tab" data-t="glosas">Glosas</button><button class="btn billing-tab ${activeTab==='recebimentos'?'active':''}" data-a="tab" data-t="recebimentos">Recebimentos</button></div>`}
function kpis(gs,rs,ds){const ready=gs.filter(g=>g.status==='ready'),sent=gs.filter(g=>['sent','processed'].includes(g.status)),received=rs.reduce((a,x)=>a+Number(x.amount||0),0),denied=ds.filter(x=>x.status!=='resolved').reduce((a,x)=>a+Number(x.amount||0),0),sentV=sent.reduce((a,x)=>a+Number(x.total_amount||0),0),open=ready.reduce((a,x)=>a+Number(x.total_amount||0),0);return `<div class="billing-kpis"><div class="billing-kpi"><small>Em aberto</small><strong>${money(open)}</strong><span>${ready.length} guias prontas</span></div><div class="billing-kpi"><small>Enviado</small><strong>${money(sentV)}</strong><span>${sent.length} guias</span></div><div class="billing-kpi"><small>Recebido</small><strong>${money(received)}</strong><span>${rs.length} recebimentos</span></div><div class="billing-kpi"><small>Glosado</small><strong>${money(denied)}</strong><span>${ds.filter(x=>x.status!=='resolved').length} em aberto</span></div></div>`}
async function render(){const host=document.getElementById('billingWorkspace');if(!host)return;host.innerHTML=CSS+'<div class="billing-card"><div class="billing-empty">Carregando faturamento…</div></div>';const[gs,ls,rs,ds]=await Promise.all([guides(),lots(),receipts(),denials()]);const nm=await names(gs.map(g=>g.patient_id));const ready=gs.filter(g=>g.status==='ready'),drafts=gs.filter(g=>g.status==='draft');let body;if(activeTab==='guias')body=renderGuides(ready,drafts,nm);else if(activeTab==='lotes')body=await renderLots(ls);else if(activeTab==='glosas')body=renderDenials(ds);else if(activeTab==='recebimentos')body=renderReceipts(rs,ls);else body=renderOverview(gs,ls,rs,ds);host.innerHTML=CSS+`<div class="billing-shell">${kpis(gs,rs,ds)}${tabs()}${body}</div>`;bind()}
function renderOverview(gs,ls,rs,ds){const ready=gs.filter(g=>g.status==='ready'),drafts=gs.filter(g=>g.status==='draft');return `<div class="billing-card"><div class="billing-head"><div><h3>Fluxo de faturamento</h3><p>Da guia TISS ao recebimento, com glosas e auditoria.</p></div></div><div class="billing-flow"><div class="billing-stage"><small>01</small><strong>Guias prontas</strong><span>${ready.length} aguardando</span></div><div class="billing-stage"><small>02</small><strong>Lotes</strong><span>${ls.length} criados</span></div><div class="billing-stage"><small>03</small><strong>Enviado</strong><span>${gs.filter(g=>g.status==='sent').length} guias</span></div><div class="billing-stage"><small>04</small><strong>Recebido</strong><span>${rs.length} registros</span></div><div class="billing-stage"><small>05</small><strong>Glosas</strong><span>${ds.filter(d=>d.status!=='resolved').length} em aberto</span></div></div></div><div class="billing-summary"><div><small>Guias prontas</small><strong>${ready.length}</strong></div><div><small>Rascunhos</small><strong>${drafts.length}</strong></div><div><small>Lotes</small><strong>${ls.length}</strong></div></div>`}
function guideRow(g,nm,ready){return `<div class="billing-row"><div class="billing-main">${ready?`<input class="billing-check billing-guide-check" type="checkbox" value="${esc(g.id)}">`:''}<div><strong>${esc(nm[g.patient_id]||'Paciente')}</strong><div class="billing-note">Guia ${esc(g.guide_number||'sem número')} · ${date(g.execution_date)} · ${money(g.total_amount)}</div></div></div><div class="billing-actions"><span class="billing-status">${status(g.status)}</span>${!ready?`<button class="btn" data-a="release" data-id="${esc(g.id)}">Liberar para faturamento</button>`:''}</div></div>`}
function renderGuides(ready,drafts,nm){return `<div class="billing-card"><div class="billing-head"><div><h3>Guias prontas para faturamento</h3><p>Selecione guias do mesmo convênio para formar um lote.</p></div><button class="btn billing-primary" data-a="create-lot" ${ready.length?'':'disabled'}>Criar lote</button></div><div class="billing-list">${ready.length?ready.map(g=>guideRow(g,nm,true)).join(''):'<div class="billing-empty">Nenhuma guia pronta para faturamento.</div>'}</div><div style="margin-top:20px;padding-top:18px;border-top:1px solid var(--border)"><div class="billing-head"><div><h3>Guias em preparação</h3><p>Rascunhos criados, ainda não liberados para faturamento.</p></div></div><div class="billing-list">${drafts.length?drafts.map(g=>guideRow(g,nm,false)).join(''):'<div class="billing-empty">Nenhum rascunho pendente.</div>'}</div></div></div>`}
async function renderLots(ls){const rows=await Promise.all(ls.map(async l=>({l,ids:await lotGuideIds(l.id)})));return `<div class="billing-card"><div class="billing-head"><div><h3>Lotes</h3><p>Acompanhe as guias agrupadas e o status do faturamento.</p></div></div><div class="billing-list">${ls.length?rows.map(({l,ids})=>`<div class="billing-row"><div><strong>${esc(l.lot_number||'Lote')}</strong><div class="billing-note">${date(l.created_at)} · ${ids.length||l.guide_count||0} guia${(ids.length||l.guide_count||0)===1?'':'s'} · ${money(l.total_amount)}</div></div><div class="billing-actions"><span class="billing-status">${status(l.status)}</span><button class="btn" data-a="open-lot" data-id="${esc(l.id)}">Abrir</button>${l.status==='draft'?`<button class="btn billing-primary" data-a="send-lot" data-id="${esc(l.id)}">Enviar</button>`:''}</div></div>`).join(''):'<div class="billing-empty">Nenhum lote criado.</div>'}</div></div>`}
async function renderDenials(ds){return `<div class="billing-card"><div class="billing-head"><div><h3>Glosas</h3><p>Registre recusas do convênio e acompanhe a resolução.</p></div><button class="btn billing-primary" data-a="new-denial">+ Registrar glosa</button></div><div class="billing-list">${ds.length?ds.map(d=>`<div class="billing-row"><div><strong>${money(d.amount)}</strong><div class="billing-note">${esc(d.reason||'Sem motivo')} · ${status(d.status)}${d.notes?' · '+esc(d.notes):''}</div></div><div class="billing-actions">${d.status==='open'?`<button class="btn" data-a="resolve-denial" data-id="${esc(d.id)}">Resolver</button>`:''}</div></div>`).join(''):'<div class="billing-empty">Nenhuma glosa registrada.</div>'}</div></div>`}
async function renderReceipts(rs,ls){return `<div class="billing-card"><div class="billing-head"><div><h3>Recebimentos</h3><p>Registre pagamentos efetivamente recebidos dos lotes.</p></div><button class="btn billing-primary" data-a="new-receipt">+ Registrar recebimento</button></div><div class="billing-list">${rs.length?rs.map(r=>{const l=ls.find(x=>x.id===r.lot_id);return `<div class="billing-row"><div><strong>${money(r.amount)}</strong><div class="billing-note">${date(r.received_at)} · ${status(r.status||'received')} · ${l?esc(l.lot_number||'Lote'):'Sem lote'}${r.reference?' · '+esc(r.reference):''}</div></div></div>`}).join(''):'<div class="billing-empty">Nenhum recebimento registrado.</div>'}</div></div>`}
async function releaseGuide(id){if(busy)return;busy=true;try{const c=S(),g=(await guides()).find(x=>x.id===id);if(!g)throw Error('Guia não encontrada.');if(Number(g.total_amount||0)<=0)throw Error('A guia precisa ter valor maior que zero.');const r=await c.from('mentalita_patient_tiss_guides').update({status:'ready',finalized_at:new Date().toISOString(),finalized_by:U()?.id||null,updated_at:new Date().toISOString()}).eq('id',id).eq('clinic_id',C()).eq('status','draft');if(r.error)throw r.error;await audit('billing_guide_released',id,g.patient_id,{amount:Number(g.total_amount||0)});await render()}catch(e){console.error(e);alert(e.message||'Não foi possível liberar a guia.')}finally{busy=false}}
async function createLot(){if(busy)return;const ids=[...document.querySelectorAll('.billing-guide-check:checked')].map(x=>x.value);if(!ids.length)return alert('Selecione pelo menos uma guia.');busy=true;try{const c=S(),gs=await guides(),picked=gs.filter(g=>ids.includes(String(g.id))&&g.status==='ready');if(!picked.length)throw Error('Somente guias prontas podem formar um lote.');const conv=[...new Set(picked.map(g=>g.convenio_id).filter(Boolean))];if(conv.length>1)throw Error('Selecione apenas guias do mesmo convênio.');const total=picked.reduce((a,g)=>a+Number(g.total_amount||0),0),lotNumber=`LOT-${new Date().toISOString().slice(0,10).replace(/-/g,'')}-${Math.random().toString(36).slice(2,7).toUpperCase()}`;const lr=await c.from('mentalita_billing_lots').insert({clinic_id:C(),lot_number:lotNumber,status:'draft',total_amount:total,guide_count:picked.length,created_by:U()?.id||null}).select().single();if(lr.error)throw lr.error;const ir=await c.from('mentalita_billing_lot_guides').insert(picked.map(g=>({clinic_id:C(),lot_id:lr.data.id,guide_id:g.id})));if(ir.error){await c.from('mentalita_billing_lots').delete().eq('id',lr.data.id);throw ir.error}for(const g of picked){const up=await c.from('mentalita_patient_tiss_guides').update({status:'batched',updated_at:new Date().toISOString()}).eq('id',g.id).eq('clinic_id',C());if(up.error)throw up.error;await audit('billing_lot_created',g.id,g.patient_id,{lot_id:lr.data.id,amount:Number(g.total_amount||0)})}activeTab='lotes';await render()}catch(e){console.error(e);alert(e.message||'Não foi possível criar o lote.')}finally{busy=false}}
async function sendLot(id){if(busy)return;busy=true;try{const c=S(),r=await c.from('mentalita_billing_lots').update({status:'sent',submitted_at:new Date().toISOString(),updated_at:new Date().toISOString()}).eq('id',id).eq('clinic_id',C()).eq('status','draft');if(r.error)throw r.error;const ids=await lotGuideIds(id);if(ids.length){const up=await c.from('mentalita_patient_tiss_guides').update({status:'sent',updated_at:new Date().toISOString()}).in('id',ids).eq('clinic_id',C());if(up.error)throw up.error;for(const gid of ids)await audit('billing_lot_sent',gid,null,{lot_id:id})}await render()}catch(e){console.error(e);alert(e.message||'Não foi possível enviar o lote.')}finally{busy=false}}
async function newReceipt(){const ls=await lots();if(!ls.length)return alert('Crie um lote primeiro.');const options=ls.map(l=>`<option value="${esc(l.id)}">${esc(l.lot_number||'Lote')} — ${money(l.total_amount)}</option>`).join('');modal(`<div class="billing-dialog-head"><h3>Registrar recebimento</h3><button class="btn" data-a="close">Fechar</button></div><div class="billing-form"><label>Lote<select id="brLot">${options}</select></label><div class="billing-form-grid"><label>Data<input id="brDate" type="date" value="${new Date().toISOString().slice(0,10)}"></label><label>Valor<input id="brAmount" type="number" min="0.01" step="0.01" placeholder="0,00"></label></div><label>Referência / comprovante<input id="brRef" placeholder="Opcional"></label><label>Observações<textarea id="brNotes" placeholder="Opcional"></textarea></label><div class="billing-dialog-actions"><button class="btn" data-a="close">Cancelar</button><button class="btn billing-primary" data-a="save-receipt">Registrar</button></div></div>`)}
async function saveReceipt(){const c=S(),lotId=document.getElementById('brLot')?.value,amount=Number(document.getElementById('brAmount')?.value||0),received=document.getElementById('brDate')?.value,reference=document.getElementById('brRef')?.value?.trim()||null,notes=document.getElementById('brNotes')?.value?.trim()||null;if(!lotId||amount<=0)return alert('Informe lote e valor válido.');const r=await c.from('mentalita_billing_receipts').insert({clinic_id:C(),lot_id:lotId,received_at:received||new Date().toISOString().slice(0,10),amount,status:'received',reference,notes,created_by:U()?.id||null});if(r.error){console.error(r.error);return alert('Não foi possível registrar o recebimento.')}const lot=(await lots()).find(x=>x.id===lotId);const receivedTotal=(await receipts()).filter(x=>x.lot_id===lotId).reduce((a,x)=>a+Number(x.amount||0),0);if(lot&&receivedTotal>=Number(lot.total_amount||0)){await c.from('mentalita_billing_lots').update({status:'paid',paid_at:new Date().toISOString(),updated_at:new Date().toISOString()}).eq('id',lotId).eq('clinic_id',C());const ids=await lotGuideIds(lotId);if(ids.length)await c.from('mentalita_patient_tiss_guides').update({status:'paid',updated_at:new Date().toISOString()}).in('id',ids).eq('clinic_id',C())}else if(lot){await c.from('mentalita_billing_lots').update({updated_at:new Date().toISOString()}).eq('id',lotId).eq('clinic_id',C())}closeModal();activeTab='recebimentos';await render()}
async function newDenial(){const ls=await lots();if(!ls.length)return alert('Crie um lote primeiro.');const gs=await guides();const options=ls.map(l=>`<option value="${esc(l.id)}">${esc(l.lot_number||'Lote')} — ${money(l.total_amount)}</option>`).join('');modal(`<div class="billing-dialog-head"><h3>Registrar glosa</h3><button class="btn" data-a="close">Fechar</button></div><div class="billing-form"><label>Lote<select id="bdLot">${options}</select></label><label>Guia (opcional)<select id="bdGuide"><option value="">Sem guia específica</option>${gs.map(g=>`<option value="${esc(g.id)}">${esc(g.guide_number||'Guia sem número')} — ${money(g.total_amount)}</option>`).join('')}</select></label><div class="billing-form-grid"><label>Valor<input id="bdAmount" type="number" min="0.01" step="0.01" placeholder="0,00"></label><label>Status<select id="bdStatus"><option value="open">Em aberto</option></select></label></div><label>Motivo<input id="bdReason" placeholder="Motivo da glosa"></label><label>Observações<textarea id="bdNotes" placeholder="Opcional"></textarea></label><div class="billing-dialog-actions"><button class="btn" data-a="close">Cancelar</button><button class="btn billing-primary" data-a="save-denial">Registrar</button></div></div>`)}
async function saveDenial(){const c=S(),lotId=document.getElementById('bdLot')?.value,guideId=document.getElementById('bdGuide')?.value||null,amount=Number(document.getElementById('bdAmount')?.value||0),reason=document.getElementById('bdReason')?.value?.trim()||null,notes=document.getElementById('bdNotes')?.value?.trim()||null;if(!lotId||amount<=0)return alert('Informe lote e valor válido.');const r=await c.from('mentalita_billing_denials').insert({clinic_id:C(),lot_id:lotId,guide_id:guideId,amount,status:'open',reason,notes,created_at:new Date().toISOString()});if(r.error){console.error(r.error);return alert('Não foi possível registrar a glosa.')}if(guideId){const g=(await guides()).find(x=>x.id===guideId);await audit('billing_denial_created',guideId,g?.patient_id||null,{lot_id:lotId,amount,reason})}closeModal();activeTab='glosas';await render()}
async function resolveDenial(id){const c=S(),r=await c.from('mentalita_billing_denials').update({status:'resolved',resolved_at:new Date().toISOString()}).eq('id',id).eq('clinic_id',C()).eq('status','open');if(r.error)return alert('Não foi possível resolver a glosa.');const d=(await denials()).find(x=>x.id===id);await audit('billing_denial_resolved',d?.guide_id||null,null,{lot_id:d?.lot_id||null,amount:Number(d?.amount||0)});await render()}
async function openLot(id){const ls=await lots(),l=ls.find(x=>x.id===id);if(!l)return;const ids=await lotGuideIds(id),gs=await guides(),nm=await names(gs.filter(g=>ids.includes(g.id)).map(g=>g.patient_id)),rs=(await receipts()).filter(r=>r.lot_id===id),ds=(await denials()).filter(d=>d.lot_id===id),sel=gs.filter(g=>ids.includes(g.id));modal(`<div class="billing-dialog-head"><h3>${esc(l.lot_number||'Lote')}</h3><button class="btn" data-a="close">Fechar</button></div><div class="billing-summary"><div><small>Status</small><strong>${status(l.status)}</strong></div><div><small>Guias</small><strong>${sel.length}</strong></div><div><small>Total</small><strong>${money(l.total_amount)}</strong></div></div><div style="margin-top:18px"><h4>Guias</h4>${sel.map(g=>`<div class="billing-row"><div><strong>${esc(nm[g.patient_id]||'Paciente')}</strong><div class="billing-note">Guia ${esc(g.guide_number||'sem número')} · ${date(g.execution_date)}</div></div><strong>${money(g.total_amount)}</strong></div>`).join('')||'<div class="billing-empty">Nenhuma guia vinculada.</div>'}</div><div style="margin-top:18px"><h4>Recebimentos</h4>${rs.map(r=>`<div class="billing-row"><div>${date(r.received_at)}${r.reference?' · '+esc(r.reference):''}</div><strong>${money(r.amount)}</strong></div>`).join('')||'<div class="billing-empty">Nenhum recebimento.</div>'}</div><div style="margin-top:18px"><h4>Glosas</h4>${ds.map(d=>`<div class="billing-row"><div>${esc(d.reason||'Sem motivo')} · ${status(d.status)}</div><strong>${money(d.amount)}</strong></div>`).join('')||'<div class="billing-empty">Nenhuma glosa.</div>'}</div>`)}
function bind(){document.querySelectorAll('[data-a="tab"]').forEach(b=>b.onclick=()=>{activeTab=b.dataset.t;render()});document.querySelectorAll('[data-a="release"]').forEach(b=>b.onclick=()=>releaseGuide(b.dataset.id));document.querySelector('[data-a="create-lot"]')?.addEventListener('click',createLot);document.querySelectorAll('[data-a="send-lot"]').forEach(b=>b.onclick=()=>sendLot(b.dataset.id));document.querySelectorAll('[data-a="open-lot"]').forEach(b=>b.onclick=()=>openLot(b.dataset.id));document.querySelector('[data-a="new-receipt"]')?.addEventListener('click',newReceipt);document.querySelector('[data-a="new-denial"]')?.addEventListener('click',newDenial);document.querySelectorAll('[data-a="resolve-denial"]').forEach(b=>b.onclick=()=>resolveDenial(b.dataset.id));document.querySelectorAll('[data-a="close"]').forEach(b=>b.onclick=closeModal);document.querySelector('[data-a="save-receipt"]')?.addEventListener('click',saveReceipt);document.querySelector('[data-a="save-denial"]')?.addEventListener('click',saveDenial)}
function build(){return `<div id="billingWorkspace"></div>`}
window.buildFaturamento=build;window.attachFaturamentoEvents=()=>render();
})();